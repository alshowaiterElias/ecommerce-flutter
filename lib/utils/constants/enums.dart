enum TextSizes { small, medium, large }

enum ProductType { variable, single }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethod { paypal, googlePlay, applePay, visa, creditCard }
